spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true #populates the data after the schema is generated by Hibernate
    show-sql: true
    hibernate:
      ddl-auto: update

